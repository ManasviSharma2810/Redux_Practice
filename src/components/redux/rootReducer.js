import {combineReducers} from 'redux';
import {reducer} from './reducer';
export default combineReducers({
  reducer,
});
//root reducer file in this file we store all reducer like we did in root navigation file
